# Локальная разработка: монтируем код + hot-reload
services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "127.0.0.1:8002:8000"
